# # multivendor_api/acckount/urls.py

from django.urls import path
# from .views import ProfileAPIView, SellerAPIView, CustomerAPIView, ProductAPIView, OrderAPIView

urlpatterns = [
#     path('profile/', ProfileAPIView.as_view(), name='profile-api'),
#     path('seller/', SellerAPIView.as_view(), name='seller-api'),
#     path('customer/', CustomerAPIView.as_view(), name='customer-api'),
#     path('product/', ProductAPIView.as_view(), name='product-api'),
#     path('order/', OrderAPIView.as_view(), name='order-api'),
]
